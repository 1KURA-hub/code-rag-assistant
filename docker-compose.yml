services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: code_rag
      POSTGRES_PASSWORD: code_rag
      POSTGRES_DB: code_rag
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
